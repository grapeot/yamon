# yamon

Mac 系统监控 TUI 工具 - 实时显示 CPU、内存、网络等系统指标

## 安装

```bash
# 使用 uv（推荐）
uv pip install -r requirements.txt

# 或使用 pip
pip install -r requirements.txt
```

## 运行

```bash
python -m yamon
```

## 功能

- CPU 占用率（每核心）
- 内存使用情况
- 网络流量（上行/下行）
- 实时更新（1秒刷新）

